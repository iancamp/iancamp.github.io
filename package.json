{
  "type": "module",
  "name": "iancamp.github.io",
  "version": "1.0.0",
  "description": "A fun, colorful, single-page site built with plain HTML/CSS/JS — ready for GitHub Pages.",
  "main": "script.js",
  "scripts": {
    "clean": "rm -rf dist",
    "copy:static-files": "mkdir -p dist && rsync -av --exclude='node_modules/' --exclude='.git/' --exclude='.github/' --exclude='dist/' --exclude='assets/photos/' --exclude='package.json' --exclude='package-lock.json' --exclude='README.md' --exclude='generate-photo-manifests.js' . dist/",
    "generate:photos": "node generate-photo-manifests.js",
    "build": "npm run clean && npm run copy:static-files && npm run generate:photos"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iancamp/iancamp.github.io.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/iancamp/iancamp.github.io/issues"
  },
  "homepage": "https://github.com/iancamp/iancamp.github.io#readme",
  "devDependencies": {
    "exifr": "^7.1.3",
    "serve": "^14.2.5",
    "sharp": "^0.33.0"
  }
}
